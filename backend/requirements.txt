# ── NVIDIA NIM Gateway — Python Dependencies ──────────────────────────
# Python 3.11+

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# HTTP / async
httpx>=0.27.0

# Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Config
pyyaml>=6.0

# Logging
loguru>=0.7.2

# Monitoring
prometheus-client>=0.20.0
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0

# Caching (optional — used for Redis backend)
# redis>=5.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0  # also for test client
